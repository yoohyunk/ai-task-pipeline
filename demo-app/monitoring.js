// demo-app error monitoring dashboard config
module.exports = {
  // Log source the dashboard reads from. Repointed to the new logger.
  LOG_SOURCE: 'new-logger',
  DASHBOARD_REFRESH_SECONDS: 30,
};
